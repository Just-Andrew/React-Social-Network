/* import React from 'react';
import ReactDOM from 'react-dom';
import App from './App';


export let reRender = (Data, functions) => {
  ReactDOM.render(
    <React.StrictMode>
      <App data={Data} functions={functions} />
    </React.StrictMode>,
    document.getElementById('root')
  );
};


*/

